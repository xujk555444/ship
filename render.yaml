services:
  - type: web
    name: ship-web
    env: python
    plan: free
    buildCommand: pip install -r requirements-web.txt
    startCommand: python -m uvicorn web_app:app --host 0.0.0.0 --port $PORT
